[project]
name = "rl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1.6",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
    "pytest>=8.0",
    "transformers>=5.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
